using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Gaia
{
    public class changesky : MonoBehaviour
    {
        public GaiaSceneLighting gaiaSceneLighting;
        public GaiaLightingProfileValues [] profiles;
        // Start is called before the first frame update
        void Start()
        {
            // GaiaSceneLighting.lightingProfile = "Day";
        }

        // Update is called once per frame
        void Update()
        {
            
        }
    }
}
